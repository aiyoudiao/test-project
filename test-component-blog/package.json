{
  "scripts": {
    "bootstrap": "npm install",
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "zip": "rimraf blog.zip && zip -r blog.zip blog/ && rimraf blog"
  },
  "devDependencies": {
    "element3": "0.0.40",
    "vuepress": "^2.0.0-beta.35"
  }
}
